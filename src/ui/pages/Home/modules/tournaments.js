export default function ({ container } = {}){
  console.log('[Home] Torneos — módulo cargado');
  if (container) container.innerHTML = `<section class="home"><h2>Torneos</h2><p>Pantalla en construcción…</p></section>`;
}
