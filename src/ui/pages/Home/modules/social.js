export default function ({ container } = {}){
  console.log('[Home] Social — módulo cargado');
  if (container) container.innerHTML = `<section class="home"><h2>Social</h2><p>Pantalla en construcción…</p></section>`;
}
