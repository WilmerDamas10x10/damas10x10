export default function ({ container } = {}){
  console.log('[Home] Ajustes & Perfil — módulo cargado');
  if (container) container.innerHTML = `<section class="home"><h2>Ajustes &amp; Perfil</h2><p>Pantalla en construcción…</p></section>`;
}
