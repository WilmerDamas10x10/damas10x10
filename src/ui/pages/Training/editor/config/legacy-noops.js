// src/ui/pages/Training/editor/config/legacy-noops.js
// No-ops para APIs V1 que ya no usamos con OV2.
// Mantener estos stubs evita romper imports antiguos.

export function markRouteLabel(/* boardEl, pos, label, opts */) {
  // noop
}

export function markStep(/* boardEl, to, n */) {
  // noop
}
