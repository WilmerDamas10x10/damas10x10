# Damas 10x10 — Full Skeleton (WAN + Botones separados)
- **WAN**: activo en todos los modos salvo **Entrenamiento**, que solo conecta al pulsar **Compartir posición**.
- **Un archivo .js por botón** en `src/ui/buttons/` (vanilla ESM, sin React).
- **Páginas** en `src/ui/pages/...` (stub por ruta).
- **Router simple** en `src/router.js`.
- Abre `public/index.html` con un server estático (p. ej. `npx serve public` o Vite).

Fecha: 2025-08-29
